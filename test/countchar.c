#include "main.h"

/**
 * _countchar - count the number of characters in a string
 * 
 * s: string to count.
 * 
 * Return: Number of characters
 */

int _countchar(char *s)
{
    int i;

    for (i = 0; s[i] != '\0'; i++)
    {
        continue;
    }
    return (i);
}